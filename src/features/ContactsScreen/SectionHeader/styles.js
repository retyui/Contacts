// @flow
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    paddingLeft: 20,
    paddingBottom: 10,
    paddingTop: 10,
    backgroundColor: '#fff',
    borderTopColor: '#00000030',
    borderTopWidth: 1,
  },
});
