// @flow
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    minHeight: 35,
    marginTop: -4,
    marginBottom: -4,
    paddingLeft: 0,
    zIndex: 1,
  },
});
